//Done

import { StyleSheet } from 'react-native';

const InputStyle = StyleSheet.create
(
  {
    input:
    {
      display: 'flex',
      width: 300,
      height: 60,
      borderWidth: 2,
      borderColor: '#CACADB',
      borderRadius: 10,
      alignSelf: 'center',
      marginVertical:22,
      textAlign: 'center'
    },
  }
);

export default InputStyle;